export default function App() {
  return (
    <div style={{ fontFamily: 'system-ui, -apple-system, Segoe UI, Roboto, Arial', padding: 24 }}>
      <h1>QA Checklist (React)</h1>
      <p style={{ opacity: 0.8 }}>Frontend is scaffolded. Backend API is available via /api/*.</p>
      <p style={{ opacity: 0.8 }}>If backend is running, the health endpoint is: <code>/api/health</code></p>
    </div>
  )
}

